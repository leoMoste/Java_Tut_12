package Stream_Collectors;
public class Main {

	public static void main(String[] args) {
		// Collector is the interface that define the general collector (4 Methods).
		// Collectors is a class that contains a lot of ready to use collectors like

		// - toList -> collects the Stream to aList, Any List
		// - toMap -> Collect the Stream into Map,
		// - toSet -> Collects the Stream into Set,
		// - joining -> create a single value by joining Items
		// - partitionBy -> uses a Predicate to partition into Map
		// - toCollection -> you can specify the List, Map classes
	}

}
